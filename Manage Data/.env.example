# Copy this file to .env only when you are ready to call OpenRouter.
# Never commit a real API key.
OPENROUTER_API_KEY=replace_with_your_openrouter_api_key
# The HTTP service reads the repository-root .env. Set CHAT_MODE=demo there
# or in the process environment for explicitly labeled integration replies.
